/**
 * data/tomografia.js
 * Educational content: CT scanner pipeline, code and explanations.
 */
'use strict';

var DATA_tomografia = {
  title:'Scanner CT Multidetector (MDCT)',
  desc:'Tubo e detectores giram a 0.25–0.5 rotações/segundo enquanto a mesa avança helicoidalmente. Retroprojeção Filtrada (FBP) ou algoritmo iterativo reconstrói fatias 3D com resolução de 0.3–0.6 mm em Unidades Hounsfield.',
  specs:[{k:'Detectores',v:'64–320 fileiras'},{k:'Rotação',v:'0.25–0.5 s/rot'},{k:'Resolução',v:'0.3–0.6 mm'},{k:'Tensão',v:'80–140 kVp'}],
  flow:[
    {t:'Aquisição Helicoidal',d:'Tubo e detectores giram continuamente no gantry. Mesa avança ao mesmo tempo — trajetória helicoidal. Pitch = avanço_por_rotação / colimação_total. Pitch 1.0 = sem sobreposição de fatias.'},
    {t:'Projeções & Sinograma',d:'Em cada posição angular, detectores registram o perfil de transmissão (N_detectores valores). Empilhar todas as projeções forma o sinograma — representação no espaço de Radon. Eixo Y = ângulo, eixo X = canal detector.'},
    {t:'Filtro Ramp (Ram-Lak)',d:'Retroprojeção simples cria borramento radial (artefato "estrela"). O filtro ramp |f| no domínio da frequência compensa isso: atenua baixas frequências (borramento) e preserva altas (bordas). Aplicado via FFT por eficiência.'},
    {t:'Retroprojeção Filtrada (FBP)',d:'Para cada pixel (x,y) e cada ângulo θ: calcula t = x·cosθ + y·sinθ (canal do detector atingido), acumula o valor filtrado. 360+ acúmulos reconstroem a imagem em valores de atenuação μ.'},
    {t:'Unidades Hounsfield',d:'HU = 1000 × (μ_tecido − μ_água) / μ_água. Ar = −1000, Água = 0, Gordura ≈ −100, Músculo ≈ +50, Osso ≈ +400 a +1000. Escala padronizada — independe do equipamento.'},
    {t:'Janelamento WW/WL & MPR',d:'O olho distingue ~30 tons de cinza; CT tem 4096 HU. Janelamento escolhe a faixa de interesse: Pulmão WW=1500/WL=−600, Osso WW=2000/WL=300, Cérebro WW=80/WL=40. MPR reconstrói planos coronais/sagitais.'},
  ],
  code:'<span class="cm">## CT — sinograma, filtro ramp, FBP, HU, janelamento</span>\n\n<span class="kw">import</span> numpy <span class="kw">as</span> np\n<span class="kw">from</span> scipy.fft <span class="kw">import</span> fft, ifft, fftfreq\n<span class="kw">from</span> scipy.ndimage <span class="kw">import</span> rotate <span class="kw">as</span> rot_img\n\n<span class="kw">class</span> <span class="cls">CTReconstructor</span>:\n  <span class="kw">def</span> <span class="fn">__init__</span>(<span class="var">self</span>, n_angles=<span class="num">360</span>, n_det=<span class="num">512</span>):\n    <span class="var">self</span>.angles = np.linspace(<span class="num">0</span>, <span class="num">180</span>, n_angles, endpoint=<span class="kw">False</span>)\n    <span class="var">self</span>.n_det  = n_det\n\n  <span class="cm">## ETAPA 1 — Transformada de Radon (sinograma)</span>\n  <span class="kw">def</span> <span class="fn">build_sinogram</span>(<span class="var">self</span>, phantom):\n    sino = np.zeros((len(<span class="var">self</span>.angles), <span class="var">self</span>.n_det))\n    <span class="kw">for</span> i, theta <span class="kw">in</span> enumerate(<span class="var">self</span>.angles):\n      rotated = rot_img(phantom, theta, reshape=<span class="kw">False</span>)\n      sino[i] = rotated.sum(axis=<span class="num">0</span>)  <span class="cm"># integra colunas</span>\n    <span class="kw">return</span> sino  <span class="cm"># shape: (n_angles, n_det)</span>\n\n  <span class="cm">## ETAPA 2 — Filtro Ramp no dominio frequencia</span>\n  <span class="kw">def</span> <span class="fn">ramp_filter</span>(<span class="var">self</span>, sinogram):\n    freqs = fftfreq(<span class="var">self</span>.n_det)\n    ramp  = np.abs(freqs)  <span class="cm"># |f|: amplifica altas freq</span>\n    filtered = np.zeros_like(sinogram)\n    <span class="kw">for</span> i <span class="kw">in</span> range(len(<span class="var">self</span>.angles)):\n      filtered[i] = np.real(ifft(fft(sinogram[i]) * ramp))\n    <span class="kw">return</span> filtered\n\n  <span class="cm">## ETAPA 3 — Retroprojacao Filtrada (FBP)</span>\n  <span class="kw">def</span> <span class="fn">backproject</span>(<span class="var">self</span>, f_sino, img_size=<span class="num">512</span>):\n    recon = np.zeros((img_size, img_size))\n    cx = cy = img_size // <span class="num">2</span>\n    x = np.arange(img_size) - cx\n    y = np.arange(img_size) - cy\n    xx, yy = np.meshgrid(x, y)\n    <span class="kw">for</span> i, theta <span class="kw">in</span> enumerate(<span class="var">self</span>.angles):\n      rad = np.deg2rad(theta)\n      <span class="cm"># t = projecao do pixel no detector</span>\n      t   = xx * np.cos(rad) + yy * np.sin(rad)\n      idx = np.clip((t + <span class="var">self</span>.n_det//<span class="num">2</span>).astype(<span class="fn">int</span>),\n                    <span class="num">0</span>, <span class="var">self</span>.n_det-<span class="num">1</span>)\n      recon += f_sino[i][idx]\n    <span class="kw">return</span> recon * np.pi / (<span class="num">2</span> * len(<span class="var">self</span>.angles))\n\n  <span class="cm">## ETAPA 4 — Converter para Hounsfield Units</span>\n  <span class="kw">def</span> <span class="fn">to_hounsfield</span>(<span class="var">self</span>, mu_map, mu_water=<span class="num">0.192</span>):\n    <span class="cm"># HU = 1000 * (mu - mu_agua) / mu_agua</span>\n    HU = <span class="num">1000.0</span> * (mu_map - mu_water) / mu_water\n    <span class="kw">return</span> np.clip(HU, -<span class="num">1024</span>, <span class="num">3071</span>)\n\n  <span class="cm">## ETAPA 5 — Janelamento WW/WL para display</span>\n  <span class="kw">def</span> <span class="fn">window_level</span>(<span class="var">self</span>, hu, WW=<span class="num">400</span>, WL=<span class="num">40</span>):\n    <span class="cm"># WW=largura de contraste, WL=centro</span>\n    <span class="cm"># Pulmao:WW=1500,WL=-600 | Osso:WW=2000,WL=300</span>\n    lo = WL - WW/<span class="num">2</span>\n    hi = WL + WW/<span class="num">2</span>\n    <span class="kw">return</span> (np.clip(hu, lo, hi) - lo) / (hi - lo)',
  explain:[
    {tag:'build_sinogram',text:'<strong>Transformada de Radon</strong>: para cada ângulo θ, rotaciona o phantom e soma as colunas — equivale a integrar a atenuação ao longo de linhas paralelas (como o feixe de raios-X faz fisicamente). O sinograma (eixo Y = ângulo, eixo X = detector) é exatamente o que o CT registra durante a rotação.'},
    {tag:'ramp_filter / |f|',text:'<strong>Por que filtrar antes de retroprojetar?</strong> Retroprojeção simples distribui cada projeção por toda a imagem uniformemente, causando borramento em "estrela". O filtro ramp <code>|f|</code> atenua baixas frequências (responsáveis pelo borramento) e preserva altas (bordas e detalhes). Aplicado via FFT para eficiência O(N log N) vs O(N²) da convolução direta.'},
    {tag:'fft / ifft',text:'<strong>Convolução via FFT</strong>: pelo Teorema da Convolução, multiplicar no domínio da frequência equivale a convolver no espaço, porém com custo O(N log N) em vez de O(N²). Para 512 canais de detector e 360 ângulos, isso representa uma aceleração de ~100× em relação à convolução direta.'},
    {tag:'backproject / t = x·cosθ',text:'<strong>FBP — Retroprojeção Filtrada</strong>: para cada pixel (x,y) e ângulo θ, a equação <code>t = x·cosθ + y·sinθ</code> calcula qual canal do detector foi atravessado pelo raio que passa por esse pixel naquele ângulo. O valor filtrado f_sino[i][t] é acumulado no pixel. Após todos os ângulos, a imagem está reconstruída.'},
    {tag:'to_hounsfield',text:'<strong>Escala Hounsfield</strong>: normaliza μ em relação à água (μ_água = 0.192 cm⁻¹ a 73 keV efetivo). Resultado: Ar = −1000 HU, Água = 0 HU, Gordura ≈ −100, Músculo ≈ +50, Osso ≈ +400 a +1000. Valores independem do equipamento — permitem comparação entre centros e ao longo do tempo.'},
    {tag:'window_level WW/WL',text:'<strong>Janelamento</strong>: CT produz 4096 HU (12 bits), mas o olho humano distingue apenas ~30 tons de cinza. O janelamento recorta a faixa de interesse: WL = centro diagnóstico, WW = largura do contraste. Pixels abaixo de lo ficam preto; acima de hi ficam branco. Médicos trocam janelas conforme a região analisada.'},
  ]
};
