/**
 * data/densitometria.js
 * Educational content: pipeline, code and explanations.
 */
'use strict';

var DATA_densitometria = {
  title:'DXA — Absorciometria Dual',
  desc:'Dois feixes de raios-X com energias distintas (40 e 80 keV) separar matematicamente osso de tecido mole. Calcula DMO (g/cm²) com dose ultrabaixa < 1 µSv — menos que um raio-X convencional.',
  specs:[{k:'Energias',v:'40 / 80 keV'},{k:'Dose',v:'< 1 µSv'},{k:'Precisão',v:'1–2% CV'},{k:'Resultado',v:'T-score / Z-score'}],
  flow:[
    {t:'Fonte Dual-Energy',d:'Gerador alterna rapidamente entre 70 e 140 kVp por chaveamento eletrônico, ou usa filtros K-edge de Samário/Cério para criar dois feixes em energias bem separadas simultaneamente.'},
    {t:'Varredura Fan-beam',d:'Gantry em arco percorre coluna L1–L4 ou fêmur proximal. Detector multicanal NaI ou CsI mede transmissão das duas energias em cada posição de varredura.'},
    {t:'Separação Matemática',d:'Com A_low e A_high medidos e os 4 coeficientes de atenuação conhecidos (μ_osso e μ_tecido nas 2 energias), resolve-se sistema linear 2×2 para obter densidades projetadas de osso e tecido mole separadamente.'},
    {t:'Cálculo de BMD',d:'BMD (g/cm²) = massa mineral total / área óssea projetada. Resultado comparado com banco normativo para calcular T-score (referência adulto jovem) e Z-score (referência mesma faixa etária).'},
    {t:'Classificação OMS',d:'T-score ≥ −1.0: Normal. Entre −1.0 e −2.5: Osteopenia. ≤ −2.5: Osteoporose. Laudos exportados via HL7 FHIR para prontuário eletrônico.'},
  ],
  code:'<span class="cm"># DXA — separacao dual-energy e calculo de BMD</span>\n\n<span class="kw">import</span> numpy <span class="kw">as</span> np\n\n<span class="kw">class</span> <span class="cls">DXAAnalysis</span>:\n  <span class="cm"># Coef. de atenuacao massica (cm2/g) por energia</span>\n  MU_BONE = {<span class="str">\'low\'</span>: <span class="num">1.38</span>, <span class="str">\'high\'</span>: <span class="num">0.52</span>}  <span class="cm"># 40/80 keV</span>\n  MU_SOFT = {<span class="str">\'low\'</span>: <span class="num">0.24</span>, <span class="str">\'high\'</span>: <span class="num">0.18</span>}  <span class="cm"># 40/80 keV</span>\n\n  <span class="kw">def</span> <span class="fn">separate_tissues</span>(<span class="var">self</span>, I_low, I_high, I0_low, I0_high):\n    <span class="cm"># Passo 1: Beer-Lambert inverso para cada energia</span>\n    A_low  = -np.log(I_low  / I0_low)\n    A_high = -np.log(I_high / I0_high)\n    <span class="cm"># Passo 2: sistema linear 2x2</span>\n    <span class="cm"># [A_low ] = [uBl uSl] * [rho_bone]</span>\n    <span class="cm"># [A_high]   [uBh uSh]   [rho_soft]</span>\n    M = np.array([\n      [<span class="var">self</span>.MU_BONE[<span class="str">\'low\'</span>],  <span class="var">self</span>.MU_SOFT[<span class="str">\'low\'</span>]],\n      [<span class="var">self</span>.MU_BONE[<span class="str">\'high\'</span>], <span class="var">self</span>.MU_SOFT[<span class="str">\'high\'</span>]],\n    ])\n    rho_bone, rho_soft = np.linalg.solve(M, [A_low, A_high])\n    <span class="kw">return</span> rho_bone, rho_soft\n\n  <span class="kw">def</span> <span class="fn">calc_bmd</span>(<span class="var">self</span>, rho_bone_map, pixel_area_cm2):\n    mass = np.sum(rho_bone_map) * pixel_area_cm2\n    area = np.sum(rho_bone_map > <span class="num">0.1</span>) * pixel_area_cm2\n    <span class="kw">return</span> mass / area  <span class="cm"># g/cm2</span>\n\n  <span class="kw">def</span> <span class="fn">classify_who</span>(<span class="var">self</span>, bmd, ref_mean, ref_sd):\n    t = (bmd - ref_mean) / ref_sd\n    <span class="kw">if</span>   t > -<span class="num">1.0</span>: <span class="kw">return</span> <span class="str">\'Normal\'</span>,      t\n    <span class="kw">elif</span> t > -<span class="num">2.5</span>: <span class="kw">return</span> <span class="str">\'Osteopenia\'</span>,  t\n    <span class="kw">else</span>         : <span class="kw">return</span> <span class="str">\'Osteoporose\'</span>, t',
  explain:[
    {tag:'MU_BONE / MU_SOFT',text:'<strong>Coeficientes de atenuação mássica (cm²/g)</strong>: divididos pela densidade do material (ao contrário do μ linear em cm⁻¹). A grande diferença entre baixa e alta energia é maior para osso (1.38 vs 0.52) do que tecido mole (0.24 vs 0.18) — isso é o que torna matematicamente possível separar os dois tecidos.'},
    {tag:'−np.log(I/I₀)',text:'<strong>Beer-Lambert inverso</strong>: converte transmissão medida (I/I₀) em "projeção de atenuação" A. A é linear com espessura e densidade — transforma o problema não-linear exponencial em um sistema linear 2×2 tratável.'},
    {tag:'np.linalg.solve',text:'<strong>Sistema linear 2×2</strong>: com dois valores medidos e quatro coeficientes conhecidos, NumPy resolve por eliminação gaussiana (decomposição LU internamente). É o coração matemático do DXA — tudo mais é apenas medida e apresentação.'},
    {tag:'calc_bmd',text:'<strong>BMD areal (g/cm²)</strong>: divisão de massa total pelo área projetada. É "areal" — não volumétrica (g/cm³). Limitação importante: pacientes mais robustos têm ossos mais espessos e podem ter BMD maior sem necessariamente ter osso mais denso.'},
    {tag:'classify_who',text:'<strong>Critério OMS 1994</strong>: T-score = (BMD − média_referência) / SD_referência. Referência = adulto jovem saudável (pico de massa óssea, 20–29 anos). Z-score usa referência da mesma faixa etária — útil para identificar perda óssea acelerada em jovens.'},
  ]
};;
