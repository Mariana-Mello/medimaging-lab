/**
 * data/mamografia.js
 * Educational content: pipeline, code and explanations.
 */
'use strict';

var DATA_mamografia = {
  title:'Mamógrafo Digital',
  desc:'Equipamento de raios-X projetado para imagens da mama com alta resolução e baixa dose. Detector de selênio amorfo (a-Se) com matriz TFT de 100 µm/pixel captura diretamente a carga gerada pelos fótons.',
  specs:[{k:'Resolução',v:'100 µm/pixel'},{k:'Tensão',v:'22–35 kVp'},{k:'Detector',v:'a-Se Flat Panel'},{k:'Dose Glandular',v:'< 3 mGy'}],
  flow:[
    {t:'Geração do Espectro X',d:'O filamento de tungstênio emite elétrons por efeito termiônico. Campo de 22–35 kV acelera os elétrons ao anodo de Ródio/Molibdênio. Colisões geram fótons por Bremsstrahlung e raios X característicos.'},
    {t:'Filtração Rh & Colimação',d:'Filtro de Ródio (K-edge 23 keV) remove fótons de baixa energia que só aumentam dose sem formar imagem. Colimador limita o feixe ao campo exato do detector.'},
    {t:'Compressão da Mama',d:'Pá de compressão aplica 10–15 kgf para uniformizar espessura, reduzir Compton scattering e aproximar tecido do detector aumentando nitidez.'},
    {t:'Detecção Direta a-Se',d:'Fótons ionizam o selênio amorfo criando pares elétron-lacuna. Campo elétrico de ~10 V/µm coleta as cargas. Cada TFT lê um pixel e ADC converte a 16 bits.'},
    {t:'DICOM & PACS',d:'Correção flat-field, LUT logarítmica e compressão. Imagem salva em DICOM (SOP Class mamografia) e enviada ao PACS via C-STORE para laudos em workstation.'},
  ],
  code:'<span class="cm"># Mamografia — pipeline de aquisição digital</span>\n\n<span class="kw">import</span> numpy <span class="kw">as</span> np\n\n<span class="kw">class</span> <span class="cls">MamographyAcquisition</span>:\n  <span class="kw">def</span> <span class="fn">__init__</span>(<span class="var">self</span>, kVp=<span class="num">28</span>, mAs=<span class="num">80</span>):\n    <span class="var">self</span>.kVp = kVp    <span class="cm"># tensão pico do tubo (kV)</span>\n    <span class="var">self</span>.mAs = mAs    <span class="cm"># carga: corrente x tempo</span>\n    <span class="var">self</span>.pixel_size = <span class="num">0.1</span>  <span class="cm"># mm (100 µm)</span>\n\n  <span class="kw">def</span> <span class="fn">generate_spectrum</span>(<span class="var">self</span>):\n    <span class="cm"># Espectro Bremsstrahlung (modelo de Kramers)</span>\n    E = np.linspace(<span class="num">1</span>, <span class="var">self</span>.kVp, <span class="num">500</span>)  <span class="cm"># energias keV</span>\n    spectrum = np.maximum(<span class="num">0</span>, <span class="var">self</span>.kVp - E)\n    <span class="cm"># Filtro Rh: remove energias &lt; 20 keV</span>\n    filter_rh = np.exp(-<span class="num">0.05</span> * np.maximum(<span class="num">0</span>, <span class="num">20</span>-E))\n    <span class="kw">return</span> E, spectrum * filter_rh\n\n  <span class="kw">def</span> <span class="fn">attenuate</span>(<span class="var">self</span>, photons, thickness_cm):\n    <span class="cm"># Lei de Beer-Lambert: I = I0 * exp(-mu*x)</span>\n    mu_tissue = <span class="num">0.45</span>  <span class="cm"># cm-1 (mama a 28 keV)</span>\n    <span class="kw">return</span> photons * np.exp(-mu_tissue * thickness_cm)\n\n  <span class="kw">def</span> <span class="fn">detect</span>(<span class="var">self</span>, transmitted):\n    <span class="cm"># a-Se: eficiencia quantica ~80%</span>\n    electrons = transmitted * <span class="num">0.80</span>\n    <span class="cm"># Ruido quantico: distribuicao de Poisson</span>\n    signal = np.random.poisson(electrons).astype(np.float32)\n    <span class="kw">return</span> signal / <span class="num">65535.0</span>  <span class="cm"># normaliza 16-bit</span>\n\n  <span class="kw">def</span> <span class="fn">log_transform</span>(<span class="var">self</span>, signal):\n    <span class="cm"># Inverte Beer-Lambert: imagem linear em espessura</span>\n    <span class="kw">return</span> -np.log(np.clip(signal, <span class="num">1e-5</span>, <span class="num">1.0</span>))',
  explain:[
    {tag:'kVp / mAs',text:'<strong>kVp (kilovolt pico)</strong>: define a energia máxima dos fótons — maior kVp penetra mais tecido. <strong>mAs</strong> = corrente × tempo: controla a quantidade de fótons. Dobrar mAs dobra a dose mas reduz o ruído por √2.'},
    {tag:'generate_spectrum',text:'<strong>Bremsstrahlung (Kramers)</strong>: fótons de raios-X têm espectro contínuo de energias, não uma única energia. A fórmula <code>kVp − E</code> aproxima essa distribuição. O filtro Rh remove energias abaixo de ~20 keV que só irradiam o paciente sem formar imagem.'},
    {tag:'Beer-Lambert',text:'<strong>I = I₀ · e^(−μ·x)</strong>: cada camada de tecido de espessura dx atenua os fótons exponencialmente. μ (cm⁻¹) é o coeficiente de atenuação linear — específico para cada tecido e energia. Equação física fundamental de todos os equipamentos de raios-X.'},
    {tag:'np.random.poisson',text:'<strong>Ruído quântico (shot noise)</strong>: fótons chegam ao detector de forma discreta e aleatória — estatística de Poisson. Com N fótons, σ = √N e SNR = √N. Mais mAs → mais fótons → melhor imagem. É o fator limitante fundamental.'},
    {tag:'log_transform',text:'<strong>Transformada logarítmica</strong>: inverte a relação exponencial de Beer-Lambert para produzir imagem com contraste linear. O <code>clip</code> para <code>1e-5</code> evita log(0) = −∞ em pixels completamente expostos.'},
  ]
};;
